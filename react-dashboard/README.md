React Trainer Dashboard (Lavender Glass)

Quick-start:

1. cd react-dashboard
2. npm install
3. npm run dev

This small scaffold keeps the original layout (sidebar, main cards, right calendar) and applies a soft purple glassmorphism theme using Tailwind. The code is intentionally small and focused on styling only — structure/content mirrors the original dashboard.
